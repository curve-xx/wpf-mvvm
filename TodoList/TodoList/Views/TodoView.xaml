<UserControl
    x:Class="TodoList.Views.TodoView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    FontSize="20"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            FontSize="24"
            Text="{Binding Title}" />
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <TextBlock
                Margin="5"
                VerticalAlignment="Center"
                Text="New Todo:" />
            <TextBox
                Width="200"
                Margin="5"
                Text="{Binding NewTodo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button
                Width="60"
                Margin="5"
                Command="{Binding AddCommand}"
                Content="Add" />
        </StackPanel>
        <DataGrid
            Grid.Row="2"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            ItemsSource="{Binding TodoModels, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{Binding IsDone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Header="Is Done" />
                <DataGridTextColumn Binding="{Binding Title, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Header="Title" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
